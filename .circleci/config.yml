version: 2.1

defaults: &defaults
  working_directory: ~/auto-demo
  docker:
    - image: circleci/node:latest-browsers

jobs:
  release:
    <<: *defaults
    steps:
      - checkout
      - run: npm ci
      - run: npx auto shipit

workflows:
  build_and_release:
    jobs:
      - release
